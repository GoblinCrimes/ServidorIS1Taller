# ServidorIS1Taller
Servidor para taller IS1

GET consigue recursos del servidor
POST manda recursos al servidor
PATCH modifica un recurso existente
DELETE borra un recurso existente

POST no es idempotente por que crear un recurso nuevo cada vez que se usa
